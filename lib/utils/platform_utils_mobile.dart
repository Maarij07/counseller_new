// Mobile-specific platform utilities implementation

/// Mobile implementation of platform utilities
class PlatformUtilsImpl {
  /// Set the document title (no-op on mobile)
  static void setDocumentTitle(String title) {
    // No-op on mobile platforms
    // Mobile apps don't have document titles like web pages
  }
}
